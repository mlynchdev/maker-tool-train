# =========================================
# Application runtime (required)
# =========================================

# App database URL
# - Local dev DB: localhost:5433
# - Docker self-hosted stack: postgres:5432
DATABASE_URL=postgres://training:training_dev_password@localhost:5433/training

# Session signing secret (use a long random value in hosted environments)
SESSION_SECRET=replace-with-random-32-char-plus-secret

# Auth provider currently implemented in app code
AUTH_PROVIDER=dev

# Public URL used by the app for links/callbacks
# - Local dev: http://localhost:3001
# - Hosted beta/prod: https://your-domain
PUBLIC_URL=http://localhost:3001

# App runtime mode
NODE_ENV=development

# =========================================
# Docker self-hosting (required for deploy/docker-compose.yml)
# =========================================

# Compose project name to isolate containers/networks/volumes
COMPOSE_PROJECT_NAME=training-staging

# Public hostname for reverse proxy certificate + routing
APP_HOST=beta.example.com

# ACME email for TLS certificate management (Caddy)
ACME_EMAIL=ops@example.com

# Postgres superuser password for the dockerized database
POSTGRES_PASSWORD=replace-with-long-random-password

# =========================================
# Optional/advanced
# =========================================

# Not implemented yet in app code; keep AUTH_PROVIDER=dev unless you add this provider.
# AUTH_PROVIDER=authentic
# AUTHENTIC_URL=https://auth.example.com

# Optional local HTTPS dev cert paths (used by Vite when set)
# VITE_DEV_HTTPS_KEY_PATH=/absolute/path/to/localhost-key.pem
# VITE_DEV_HTTPS_CERT_PATH=/absolute/path/to/localhost.pem

# Optional default makerspace timezone used before admin config is saved
# Must be an IANA timezone, e.g. America/Los_Angeles
# MAKERSPACE_TIMEZONE=America/Los_Angeles
